apiVersion: v1
kind: Service
metadata:
  name: {{ include "ngx-blog.fullname" . }}
  labels:
    {{- include "ngx-blog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ngx-blog.selectorLabels" . | nindent 4 }}
