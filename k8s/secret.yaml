apiVersion: v1
kind: Secret
metadata:
  name: ngx-blog-secret
  namespace: ngx-blog
type: Opaque
stringData:
  # Base64 encode these in production or use external secrets manager
  DATABASE_URL: "postgresql://user:password@postgres:5432/ngx_blog"
  JWT_SECRET: "your-super-secret-jwt-key"
